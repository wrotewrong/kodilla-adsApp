/* SELECTORS */

/* ACTIONS */

// action name creator

/* THUNKS */

/* INITIAL STATE */

const initialState = {
  data: [],
  request: {},
};

/* REDUCER */

export default function reducer(statePart = initialState, action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
